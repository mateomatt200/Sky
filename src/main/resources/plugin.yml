name: SimpleSky
version: '${project.version}'
main: com.venned.simplerealm.Main
api-version: '1.20'
commands:
  unclaim:
  claim:
  realm:
  home:
  visit:
  configrealm:
  border:
depends: [SimpleInventory,WorldEdit,Vault]
softdepend: [PlaceholderAPI]